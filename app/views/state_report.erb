<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <link rel="stylesheet" href="/css/bahunya.css" />
</head>
<body> 
    <style>
        .box {
            display:flex;
            justify-content:center;
        }

        .a, .b {
            margin-right:auto;
        }
    </style>

    <div class="box"><h2 class="a">Logo</h2><h1 class="b">State Report</h1></div>
        <%@result.each_with_index do |office, index| %>
            <h2><%= office[index]["state"] %></h2>
            <table>
                <%office.each do |data| %>
                    <tr>
                        <td><%= data["title"] %></td>
                        <td><%= data["type"] %></td>
                        <td><%= data["address"] %></td>
                        <td><%= data["lob"] %></td>
                    </tr>
                <% end %>
            </table>
        <% end %>
</body>
</html>